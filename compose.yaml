services:
  frontend:
    image: abhishekjwp/interview_app-frontend:latest
    ports:
      - "80:80"

  backend:
    image: abhishekjwp/interview_app-frontend:latest
    ports:
      - "5000:5000"
    env_file:
      - ./.env

  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: